<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.lby.meiqia.user.mapper.DoctorMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="org.lby.meiqia.user.entity.Doctor">
        <id column="user_id" property="userId" />
        <result column="card" property="card" />
        <result column="type_id" property="typeId" />
        <result column="adept" property="adept" />
        <result column="seniority" property="seniority" />
        <result column="room_id" property="roomId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        user_id, card, type_id, adept, seniority, room_id
    </sql>
    <select id="page" resultType="org.lby.meiqia.user.vo.DoctorVO">
    private Long doctorId;
    private String name;
    private Integer level;
    private Integer room;
    private Integer price;
        select a.*
        from t_

    </select>
</mapper>
